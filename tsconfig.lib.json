{
    // Build-only tsconfig for ng-packagr. Peers are named as BUILT output, not
    // sources: compiling a peer's sources in puts them outside this package's
    // rootDir (TS6059) and ships a second copy of that peer to anyone who
    // installs both.
    "compilerOptions": {
        "target": "ES2022",
        "module": "ES2022",
        "moduleResolution": "bundler",
        "lib": ["ES2022", "dom"],
        "strict": true,
        "noImplicitOverride": true,
        "noPropertyAccessFromIndexSignature": false,
        "noImplicitReturns": true,
        "noFallthroughCasesInSwitch": true,
        "skipLibCheck": true,
        "esModuleInterop": true,
        "experimentalDecorators": true,
        "importHelpers": true,
        "declaration": true,
        "sourceMap": true,
        "paths": {
            "@coolms/core-angular": ["../core-angular/dist"],
            "@coolms/ui-angular": ["../ui-angular/dist"],
            // OPTIONAL peer: only the cell font picker reads it, and it is
            // loaded dynamically so a consumer without it still gets an
            // editor. Named here for the same reason as the others -- the
            // package resolves its siblings from their built output, never
            // from npm, because `node_modules` is a symlink to the admin's
            // and exactly one Angular tree may be in play.
            "@coolms/document-engine": ["../document-engine/dist"]
        }
    },
    "angularCompilerOptions": {
        "compilationMode": "partial",
        "strictInjectionParameters": true,
        "strictTemplates": true
    },
    "include": ["src/**/*.ts"],
    "exclude": ["src/**/*.spec.ts"]
}
